// ── STATE ─────────────────────────────────────────────────────────────────────
// Exports the global state (which is held on the window object for seamless
// backward compatibility with direct global variable access in other modules).

export const state = window;
